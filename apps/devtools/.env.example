# Devtools app (Vite) environment variables
#
# Vite exposes variables prefixed with VITE_* to the client bundle.
# Anything here is shipped to users — never put secrets here.

# Base URL for the devtools app to call the server.
VITE_API_BASE_URL=http://localhost:8080
